# 优化替换规则布局 - 一行显示所有元素

## 📋 优化目标
将规则编号、被替换内容输入框、替换为输入框、删除按钮都放在同一行，去掉"替换规则"标题，使布局更紧凑。

## 🎯 实现步骤

### 1. 修改 BatchTextReplace.vue 的规则区域结构

**新的布局**：
```vue
<!-- 替换规则区域 - 支持多条规则 -->
<div class="replace-rules-section">
  <button @click="addRule" class="add-rule-btn">
    <span class="btn-icon">➕</span>
    <span class="btn-text">添加规则</span>
  </button>
  
  <div class="rules-list">
    <div 
      v-for="(rule, index) in rules" 
      :key="index" 
      class="rule-item"
    >
      <div class="rule-number">{{ index + 1 }}</div>
      <div class="rule-inputs">
        <input 
          v-model="rule.searchText" 
          placeholder="被替换内容"
          class="rule-input search-input"
        />
        <input 
          v-model="rule.replaceText" 
          placeholder="替换为"
          class="rule-input replace-input"
        />
      </div>
      <button @click="removeRule(index)" class="remove-rule-btn" v-if="rules.length > 1" title="删除此规则">
        🗑️
      </button>
    </div>
  </div>
</div>
```

**关键改动**：
- 删除 `.rules-header`（包含"替换规则"标题和"添加规则"按钮）
- 将"添加规则"按钮移到规则列表上方，独立显示
- 规则项改为一行：编号 + 两个输入框 + 删除按钮
- 输入框去掉 label，使用 placeholder
- 更紧凑的间距和 padding

### 2. 更新样式

**样式优化**：
```css
.replace-rules-section {
  margin-bottom: 20px;
  flex-shrink: 0;
}

.add-rule-btn {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  padding: 10px 16px;
  background: var(--primary-color);
  color: white;
  border: none;
  border-radius: var(--radius-md);
  cursor: pointer;
  font-size: 0.85rem;
  font-weight: 600;
  transition: all 0.2s ease;
  margin-bottom: 12px;
}

.add-rule-btn:hover {
  background: var(--primary-dark);
  transform: translateY(-1px);
  box-shadow: 0 2px 8px rgba(99, 102, 241, 0.3);
}

.rules-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.rule-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 12px;
  background: var(--bg-secondary);
  border-radius: var(--radius-md);
  border: 2px solid var(--border-color);
}

.rule-number {
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 40px;
  padding: 4px 8px;
  background: var(--primary-color);
  color: white;
  border-radius: var(--radius-sm);
  font-weight: 600;
  font-size: 0.75rem;
  flex-shrink: 0;
}

.rule-inputs {
  flex: 1;
  display: flex;
  gap: 8px;
}

.rule-input {
  flex: 1;
  padding: 8px 12px;
  border: 2px solid var(--border-color);
  border-radius: var(--radius-md);
  font-size: 0.85rem;
  background-color: var(--bg-primary);
  color: var(--text-primary);
  font-family: inherit;
  height: 36px;
  transition: all 0.2s ease;
}

.rule-input:focus {
  outline: none;
  border-color: var(--primary-color);
  box-shadow: 0 0 0 3px var(--primary-light);
}

.remove-rule-btn {
  background: none;
  border: none;
  font-size: 1rem;
  cursor: pointer;
  padding: 4px;
  opacity: 0.6;
  transition: all 0.2s ease;
  flex-shrink: 0;
}

.remove-rule-btn:hover {
  opacity: 1;
  transform: scale(1.1);
}
```

### 3. 删除旧样式

**删除的样式**：
- `.rules-header`
- `.rules-title`
- `.input-group`（在规则项内部）
- `.rule-item` 中的 label

## 📁 修改文件
1. **修改**: `src/components/BatchTextReplace.vue`
   - 模板部分：重新组织规则区域布局
   - 样式部分：更新规则相关样式，删除不需要的样式

## ✅ 预期效果
- 规则编号、两个输入框、删除按钮都在一行
- 去掉"替换规则"标题，更简洁
- 布局更紧凑，空间利用更高效
- 适合快速输入词语和标点符号替换